// import CountryContext from "./CountryContext";
// import { useContext } from "react";

// export const searchCountry = (id) =>{
//     const { results, dispatch } = useContext(CountryContext);

//     let country = results.find(country => country.id === id)
//     dispatch({type: "GET_CONCERNED_COUNTRY", payload: country})
// }

